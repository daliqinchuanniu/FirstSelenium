package pages.personal;

import framework.BasePage;
import org.openqa.selenium.WebDriver;

public class FavorityPage extends BasePage {
    public FavorityPage(WebDriver driver) {
        super(driver);
    }
}
